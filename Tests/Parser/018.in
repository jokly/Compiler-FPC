program hello;

type
    IntArr = array[1..100] of Integer;

function getArr(a, b: Integer) : IntArr;
begin

end;

var
    arr: array[1..3] of array[1..3] of array[1..3] of Integer;
    int, i: Integer;

begin
    int := arr[1, i + 2, 2 * 3 - i];
    int := getArr(0, 1)[i * 2];
end.